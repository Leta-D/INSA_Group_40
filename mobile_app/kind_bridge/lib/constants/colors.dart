import 'package:flutter/material.dart';

var appDarkGreen = (double opacity) => Color.fromRGBO(44, 72, 44, opacity);
var appWhite = (double opacity) => Color.fromRGBO(255, 255, 255, opacity);
var appBlack = (double opacity) => Color.fromRGBO(10, 10, 10, opacity);
var appRed = (double opacity) => Color.fromRGBO(226, 8, 26, opacity);
