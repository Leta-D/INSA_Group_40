import 'package:flutter/material.dart';

class DonorHomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Welcome to the Donor Home Page!')),
    );
  }
}
